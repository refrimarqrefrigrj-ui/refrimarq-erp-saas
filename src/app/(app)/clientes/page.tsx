/**
 * Placeholder da página de Clientes. Será substituída pela listagem real
 * (tabela + busca + cadastro) na etapa de UI do Módulo 1.
 */
export default function ClientesPage() {
  return (
    <div className="mx-auto w-full max-w-6xl p-6">
      <h1 className="text-2xl font-semibold tracking-tight">Clientes</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Em construção — a listagem e o cadastro de clientes chegam nesta etapa.
      </p>
    </div>
  );
}
